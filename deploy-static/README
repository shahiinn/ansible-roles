Overview:
  - To copy static file to remote machine
  - If the static files are executing as node server
  	* Start the service
  	* Wait for the service to start

Steps:
  - Copy static files from Jenkins to the remote machine.
  - If the static files are running as node server 
	* Copy the init/service script for running the node server as service.
  - 	* Start the service, by executing the service. The service will be executed as the user specified in the init script.
  - 	* Check the port on which the service (node server) is listening.

Supported Platform:
  - Currently the init script works only on the Ubuntu 14.04, not tested in Ubuntu 16+ and on rpm based linux systems.

